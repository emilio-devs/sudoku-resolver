[project]
name = "sudoku-studio"
version = "0.1.0"
description = "Laboratorio local para experimentar con algoritmos de Sudoku"
requires-python = ">=3.12"

[tool.pyright]
include = ["main.py", "sudoku", "soluciones", "ui", "tests"]
exclude = [".venv", "artifacts", "data"]
typeCheckingMode = "basic"
